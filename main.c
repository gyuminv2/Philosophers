#include "philo.h"

int	main(int ac, char *av[])
{
	t_all	all;

	//if (check_ac_av(ac, av) != 0)
	//	return (-1);
	//init_all(all, ac, av);
	//launch_threads
	//join_threads
	//free_all(all);
	return (0);
}
